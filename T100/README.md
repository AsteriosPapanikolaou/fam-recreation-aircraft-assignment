# T-100 data

This folder contains the processed T-100 route-demand tables used to estimate average passengers per flight and spill costs.

The primary model input is `envoy_t100_jan2023_demand.csv`.
