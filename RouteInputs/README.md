# Route and assignment inputs

This folder contains route-level demand/fare inputs and the final flight-aircraft cost matrix.

- `route_inputs_jan2023.csv` and `route_inputs_jan2023_with_spill.csv` contain route-level demand and fare features.
- `envoy_jan2023_initial_assignment_costs.csv` contains operating, spill and total costs for every flight and aircraft type.
