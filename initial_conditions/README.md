# Initial fleet conditions

These tables define the starting aircraft inventory for the time-space FAM.

- `envoy_tail_initial_positions_proxy.csv` records the tail-level proxy reconstruction.
- `envoy_initial_aircraft_positions.csv` aggregates that proxy by aircraft type and airport.
- `envoy_initial_aircraft_positions_feasible.csv` is the repaired distribution used by the final model run.
